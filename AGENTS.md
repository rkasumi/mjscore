# AGENTS.md

## 禁止/要確認/許可
- 禁止: 秘密情報の追加/コミット（.env 実体不可、サンプルのみ）
- 禁止: 依存更新や大規模改修（依頼がない限り）
- 要確認: 既存構成/命名/公開IFの変更
- 要確認: 破壊的変更・外部影響のある操作
- 許可: 既存方針に沿った小さな修正/改善

## 作業開始ルーチン
- 変更対象・影響範囲を確認
- 既存実装優先で最小差分
- 追加/削除が必要なら理由を短く説明

## DoD
- `pnpm -s check` が通る状態にする（難しければ理由を記載）
- 影響点と実行/確認手順を短く残す

## 優先順位（既存実装優先）
- 既存設計/命名/構成を優先
- 新規導入より既存ライブラリ/仕組みを優先
- 例外は理由と影響範囲を短く説明

## AI_RULES.md統合（廃止）
- 旧 AI_RULES.md は廃止し、AI_RULES.md.deprecated に保管済み
- 手順・コマンドは README.md / docs を参照


## Repo Notes
- TypeScript strict、any 禁止。UI は Tailwind のみ
- 既存フォルダ構成・export 方針は維持する
- デプロイ: フロントは dist を scp 配置、バックは git pull 後に `podman-compose up -d --build`
