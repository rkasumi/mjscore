- TypeScript strict。any禁止（必要なら型を定義する）
- 既存のフォルダ構成・ファイル名・export方針を変更しない
- UIは Tailwind のみ（新しいUIライブラリを勝手に追加しない）
- モジュールの追加は最小限とする
- 1ファイルが長くなったら分割（目安200行）
- 変更後は **pnpm -s check（lint→build）**が通る状態にしてから完了
- 例外（設計変更）が必要な時は、まず理由と影響範囲を短く説明してから変更する