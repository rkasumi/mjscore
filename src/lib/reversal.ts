export type ReverseCondition = {
  playerId: string;
  targetPoint: number;
};

export const calculateReverseConditions = (): ReverseCondition[] => {
  // TODO: Implement reverse condition calculation.
  return [];
};
